# Databricks Configuration
DATABRICKS_PROFILE="your-profile"
DATABRICKS_HOST="https://your-workspace-name.cloud.databricks.com"
# DATABRICKS_CLUSTER_ID="your-cluster-id"  # Optional - Comment out to use serverless clusters 

# MLflow Configuration
MLFLOW_TRACKING_URI="databricks://your-profile"
# For Unity Catalog model registry (recommended):
MLFLOW_REGISTRY_URI="databricks-uc"
# OR for legacy workspace model registry:
# MLFLOW_REGISTRY_URI="databricks://your-profile"

# Unity Catalog Configuration (if using UC)
UC_CATALOG="main"
UC_SCHEMA="default"

# Optional: Experiment configuration
MLFLOW_EXPERIMENT_NAME="/Workspace/Users/username@yourcompany.com/test-mlflow-remote"
